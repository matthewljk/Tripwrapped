# Copy to .env.local and fill in values. Do not commit .env.local or real keys.

# Mapbox (map UI: Wrap It Up). Get token: https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Google Maps (Daily Journal POI names via Places API). Server-side only – used by /api/places/nearby.
# Create key: Google Cloud Console → APIs & Services → Credentials. Enable "Places API (New)".
# Restrict key to your domain and/or IP in production.
GOOGLE_MAPS_API_KEY=
